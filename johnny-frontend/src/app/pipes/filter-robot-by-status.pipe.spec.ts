import { FilterRobotByStatusPipe } from './filter-robot-by-status.pipe';

describe('FilterRobotByStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRobotByStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
