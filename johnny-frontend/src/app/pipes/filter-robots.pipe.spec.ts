import { FilterRobotsPipe } from './filter-robots.pipe';

describe('FilterRobotsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRobotsPipe();
    expect(pipe).toBeTruthy();
  });
});
